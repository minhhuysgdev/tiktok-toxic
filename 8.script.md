# scripts/start_streaming.sh
#!/bin/bash
spark-submit \
  --packages org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.1,org.postgresql:postgresql:42.7.3 \
  --jars /path/to/transformers-jars-if-needed \
  src/speed_layer/streaming_toxicity.py

# scripts/run_batch.sh
#!/bin/bash
spark-submit \
  --packages org.postgresql:postgresql:42.7.3 \
  src/batch_layer/batch_full_recompute.py

# Crontab mẫu
0 2 * * * /path/to/scripts/run_batch.sh >> /var/log/tiktok_batch.log 2>&1